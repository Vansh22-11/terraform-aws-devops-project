apiVersion: v1
kind: Service

metadata:
  name: terraform-devops-service

spec:
  type: NodePort

  selector:
    app: terraform-devops-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080